package mybase;

/**
 * Created by thoma on 19-Mar-17.
 */
public class WallCollisionBehavior extends EntityCollisionBehavior {
    public WallCollisionBehavior(Entity entity, Game game) {
        super(entity, game);
    }
    // collisions handled in other classes
}
